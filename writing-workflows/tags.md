# Tags

`tags` is deprecated for DAG metadata. Use [`labels`](/writing-workflows/labels) instead.

Existing DAG YAML that uses `tags` continues to load for compatibility, but new examples and API requests should use `labels`.
